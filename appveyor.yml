image:
  - Visual Studio 2015

environment:
  BOOST_ROOT: C:\Libraries\boost_1_63_0
  
build_script:
  - cmd: build.bat Release
  - cmd: cd build\win_32-x86_64\Release && msbuild amis_poc.sln && ctest -C "Release"
